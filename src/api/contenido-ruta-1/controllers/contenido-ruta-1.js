'use strict';

/**
 * contenido-ruta-1 controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::contenido-ruta-1.contenido-ruta-1');
